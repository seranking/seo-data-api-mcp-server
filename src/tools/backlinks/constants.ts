export const MODES = [
    "domain",
    "host",
    "url"
] as const;

export const OUTPUT_FORMATS = [
    "json",
    "xml"
] as const;

export const URL_FILTER_MODES = [
    "contains",
    "does_not_contain"
] as const;